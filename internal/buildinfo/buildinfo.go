package buildinfo

// Version is injected by release builds. The source default tracks the current
// package release so local runtimes can expose the correct UI version.
var Version = "0.1.4"

const Repository = "WiltonH/Punkdom"
